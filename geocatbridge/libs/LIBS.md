# Library folder

This folder contains Python libraries on which GeoCat Bridge depends, and for which a specific version is required.  

Python package sources are copied into this folder after a git submodule checkout when the `/scripts/submodule_update.py` script is used.