"""
TODO
Move (new version of) GeoNetwork, GeoServer etc. modules to the models folder.
Make sure that they all inherit from ServerBase in the new servers/bases.py.
Each module should come with a *.ui file (store in UI folder or here?) that the ServerConnectionsWidget can use.
"""
